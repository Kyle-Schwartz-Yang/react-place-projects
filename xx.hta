<html>
<head>
<title>Підтвердження дії</title>
<HTA:Application
    border="thick"
    borderStyle="raised"
    contextMenu="no"
    innerBorder="yes"
    maximizeButton="no"
    minimizeButton="no"
    navigable="yes"
    scroll="no"
    selection="no"
    singleInstance="yes"
    sysMenu="yes"
/>
<style>
    body { font-family: Segoe UI, Tahoma, sans-serif; background-color: #f0f0f0; margin: 0; padding: 20px; text-align: center; }
    .message { font-size: 16px; margin-bottom: 20px; color: #333; }
    .button-container { display: flex; justify-content: center; gap: 15px; }
    .action-button {
        padding: 10px 25px;
        font-size: 14px;
        cursor: pointer;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }
    .yes-button { background-color: #0078d7; color: white; }
    .yes-button:hover { background-color: #005a9e; }
    .no-button { background-color: #ccc; color: #333; }
    .no-button:hover { background-color: #b3b3b3; }
</style>
<script language="VBScript">
Sub ConfirmedAction(action)
    If action = "Yes" Then
        ' ВАЖЛИВО: Замініть команду на потрібну (наприклад, "shutdown /h" або "shutdown /s /t 0")
        CreateObject("WScript.Shell").Run "rundll32 user32.dll,LockWorkStation", 0, False
    End If
    ' Закрити HTA-вікно після натискання кнопки
    window.close
End Sub
</script>
</head>
<body>
    <div class="message">Ви впевнені, що хочете заблокувати комп'ютер?</div>
    <div class="button-container">
        <button class="action-button yes-button" onclick="ConfirmedAction('Yes')">Так, заблокувати</button>
        <button class="action-button no-button" onclick="ConfirmedAction('No')">Ні, скасувати</button>
    </div>
</body>
</html>